# 改訂版設計（Subagent 2: フロントエンド・HIG・スプシ風タイムライン）— QA監査反映

**結論（3行）**: 監査指摘7件を全件受諾し設計へ反映した（却下ゼロ。#7のみ「スコープ外裁定＋最小受け皿の提案」として反映）。骨格（仮想スクロール不採用・自前SVG・5タブ・tokens置換マップ）は不変で、追加は「操作者ピッカー＋明示既読」「入力封鎖フラグ」「外出・外泊の入力/表示動線」「下書き2レイヤー規則」「チップ44pxヒット」の5点。確信度: 高（content-visibility の施設iPad実機対応と、操作者再確認の頻度設定のみ未検証/要承認）。

---

## 監査対応（指摘→対応の対照）

| # | 判定 | 対応（反映先） |
|---|---|---|
| 1 [high] | **受諾** | 操作者（記入者）ピッカーを新設。`cl_staffId` 数値のみ保持・staffスナップショット照合・既読は明示操作限定。→ 新設 §0・§6・§9 |
| 2 [high] | **受諾** | `app_settings` の入力解禁フラグでフロント側の入力UIを封鎖（閲覧・検証専用モード）。UIガード＋書込関数ガードの二重化。importer側の skip 計数は Subagent 1 管轄と明記。→ 新設 §0.5・§6 |
| 3 [medium] | **受諾** | 「3系列並列 or RPC1発」の記述を撤回し、**初期10日・追加10日・RPC1発（構成データ7種）** に統一。→ §3 |
| 4 [medium] | **受諾** | 「送信失敗キュー（保持必須）」と「入力途中下書き（成功/明示破棄で即削除＋24h期限）」の2レイヤーに分離し、§9の「保存しない」との矛盾を解消（原則11のUI状態とは別レイヤーと定義）。プレフィクスは `cl_` に統一（`clg_` 廃止）。→ §6.5・§9 |
| 5 [medium] | **受諾** | サマリチップは見た目32px維持＋擬似要素で縦ヒット44px化（横は gap 8px 維持でヒット重複なし）。hig-audit critical 検証対象に明記。→ §2 |
| 6 [medium] | **受諾** | 外出・外泊の日内ブロック＋サマリチップ＋記録ハブ第4ボタン＋帰着未定（end null）の後追い記入UIを追加。食事 status との自動連動はしない（表示併記のみ）。→ §2・§6 |
| 7 [medium] | **受諾（裁定明示）** | 出勤者ブロックは**本アプリのスコープ外と明示裁定**。代替はワースケ/シフトアプリ参照。必要時のみ app_settings 日次テキスト1行の最小受け皿（表示1行・折りたたみ）を追加できる設計余地を確保。承認時に本人確認。→ §2 末尾・残課題 |

---

## 0. 操作者（記入者）モデル【監査#1対応・新設】

共有アカウント案を既定とし、**「認証（Supabase Auth）」と「操作者（誰として記録・既読するか）」を別レイヤー**に分ける。個別アカウント案に裁定された場合は本節の操作者=Auth ユーザーに縮退（ピッカー廃止・照合ロジック流用）。

- **操作者ピッカー画面**: 職員一覧（staffスナップショット由来・かな絞込付き・各行 min-height 44px）。表示タイミング: ①初回起動 ②`cl_staffId` 照合失敗時 ③ヘッダの操作者チップをタップした明示切替時 ④再確認条件成立時。
- **再確認シート**: 起動時に保持 staff_id が有効なら「（職員名）として記録します［続ける／切り替える］」のワンタップ確認。毎回は煩雑なため、**日付が変わった or 最終操作から4時間経過**の場合のみ表示する案（頻度は承認時に1点確認）。誤帰属（別職員名義の記録・既読）は記録の信頼性を毀損するため、M-034（二重照合）の精神で明示確認を挟む。
- **保持**: `cl_staffId` に **staff_id 数値のみ**（氏名・かな等は一切保持しない）。起動時に Supabase 側 staff スナップショットとホワイトリスト照合し、不在・無効値・退職済みはピッカーへフォールバック（壊れた値で起動不能にしない）。
- **ヘッダ常時表示**: スティッキーヘッダ右に操作者チップ（氏名・タップで切替）。シフト交代時の切替導線を1タップに置く。
- **既読付与は明示操作のみ**: 本文展開タップ or カード内「既読にする」ボタン（44px）で `note_reads(note_id, staff_id)` を書く。**一覧レンダリングや自動スクロール到達では書かない**（multi-device-sync 原則9: 表示系から書き込まない。誤既読=未読喪失の防止）。一括既読は初版では設けない（誤タップで未読喪失＝破壊的操作に近いため。要望があれば確認ダイアログ付きで追補）。
- 「✓既読 5」タップで既読職員一覧をポップオーバー表示（職員名のみ・入居者情報を含まないため覗き見配慮の対象外）。
- 申し送りフォームの記入者欄は**現在の操作者を初期値**（変更可・ピッカー同一部品）。

## 0.5 入力解禁フラグ（並走期間の封鎖）【監査#2対応・新設】

- `app_settings` の入力解禁フラグ（切替日D or boolean。キー定義は Subagent 1 管轄）を起動時＋記録タブ表示時に**毎回取得**（「前提情報は毎回取り直す」規範。Realtime 購読があれば即時追従）。
- **封鎖中のUI**: 記録タブ・「＋申し送り」ショートカット・外出外泊入力は**隠さずディセーブル＋理由文**「現在はスプレッドシートで記録する期間です（アプリ入力の開始日は施設で決定します）」。タイムライン・検索・カルテは全機能有効（閲覧・検証専用モード）。
- **二重ガード**: UIの封鎖だけに依存せず、全書込関数の入口で共通ガード（フラグ再検証→不許可なら書込せずエラー表示）。**最終強制は RLS/DB 側**（Subagent 1 管轄）で、フロントは早期失敗と説明文を担当。
- importer の skip 計数（M-024計数式）・部分uniqueの衝突規則は Subagent 1/3 管轄のため本設計からは参照のみ。

## 1. 画面構成とナビゲーション

- 構成: `<html data-ds="ops">`＋スティッキーヘッダ（画面名・保存状態・**操作者チップ**・検索アイコン）＋固定ボトムタブ5個（house流儀 care-tools-html.md の .hdr/.bnav 型を React で踏襲）。
- 5タブ: **①タイムライン（既定）②記録（入力ハブ）③カルテ ④検索 ⑤設定**。各タブ 高さ56px・アイコン＋文字ラベル併記（アイコンのみ禁止）。
- 画面幅による変形: <768px=ボトムタブ／≥1024px（iPad横・PC）=左レール（同じ5項目・位置一貫）。iPad縦(834px)はボトムタブのまま。片手操作の主導線（記録・タイムライン）は下部＝親指圏。
- 動線（すべて2〜3タップ）:
  - 定時バイタル: 記録タブ→「バイタル」→グリッド＝**2タップ**
  - 申し送り記入: 記録タブ→「申し送り」＝**2タップ**（タイムライン各日ヘッダにも「＋申し送り」ショートカット=1タップ）
  - 外出・外泊: 記録タブ→「外出・外泊」＝**2タップ**（帰着記入はタイムライン外出ブロックの「帰着」ボタン=1タップ）【#6】
  - 個人カルテ: カルテタブ→利用者一覧（居室順・氏名インクリメンタル絞込付き）→本人＝**2タップ**。タイムライン上の氏名タップからも直行=1タップ
  - 検索: 検索タブ＝1タップ→入力
  - 操作者切替: ヘッダ操作者チップ＝**1タップ**【#1】
- 記録ハブ画面: 「バイタル一括」「食事一括」「申し送り」「外出・外泊」の**4大ボタン2×2グリッド**（各 min-height 72px・アイコン＋17px文字・gap 8px以上）【#6】。

## 2. スプシ風タイムライン詳細レイアウト（HTMLモック起こし用）

日付逆順の縦1カラム（max-width 720px 中央、iPad横では右に当日サマリ用サイドパネル可＝初版は1カラムで確定）。

```
[日付セクション]（1日=1 <section>、content-visibility:auto 対象）
├ 日付ヘッダ（sticky top:ヘッダ高, 高さ48px, bg:--c-surface2, border-bottom:--c-border-strong）
│   「8/27（水）」--fs-lg --fw-heavy ＋ 状態バッジ（✓取込済 / 網掛け＋「未取込」）
│   ＋サマリチップ列（--fs-sm・見た目 min-height 32px・::before で縦ヒット44px化【#5】
│     隣接チップは gap:--tap-gap（8px）・ヒット拡張は縦方向のみで横の重複なし
│     タップで該当ブロックへスクロール）:
│     「申し送り 12件（未読3）」「▲発熱 2名」「▲低摂取 3名」「外出 2名」【#6】「— 未測定 1名」
├ ピン留めブロック（重要度=最重要 or 継続フラグ有効中のみ表示。bg:--c-warn-bg, 左罫4px --c-warn）
│   行: 「‼」記号＋対象＋本文1行clamp＋「継続 〜8/31」
├ 申し送りリスト（勤務帯グループ見出し: 日勤／デイ／夜勤、--fs-2xs --c-ink3）
│   カード（bg:--c-surface, radius:--r-md, padding:--sp-3, 間隔:--sp-2, min-height:56px）:
│   1行目: 時刻(tabular-nums) | 対象（利用者名 --fw-bold or「スタッフへ」--c-info+ⓘ）| 職種タグchip | 重要度記号（無印/▲重要(warn)/‼最重要(danger)）
│   2行目: 本文 --fs-base 2行clamp（覗き見配慮。タップで展開＝明示操作・展開時に既読付与【#1】。展開状態は保存しない）
│   3行目: 記入者・「✓既読 5」（タップで既読者一覧）・「既読にする」44pxボタン
│         （自分=操作者が未読のカードは左罫3px --c-accent＋「未読」文字）
├ 外出・外泊ブロック（見出し「外出・外泊 2名」。該当者がいる日のみ表示）【#6】
│   行: 氏名 | 区分（外出/外泊 文字chip）| 時刻範囲(tabular-nums・帰着未定は「帰着未定」文字+--c-warn-bg)
│      | 付添 | ［帰着］ボタン（end null の行のみ・44px・タップで帰着時刻入力）
├ バイタルブロック（見出し「バイタル ▲異常2名・✓31名正常・—未測定0」）
│   既定=異常者のみの行表示: 居室 | 氏名 | KT | 上/下 | P | SpO2（超過セルのみ 記号+色+bg。例「38.4↑↑」文字--c-danger/bg--c-danger-bg、「35.2↓↓」--c-info系=凡例の青を info に割当）
│   「全33名の表を見る」ボタン→日内アコーディオン展開（スプシ同型の全員表・行高44px・居室昇順・空欄は「—」表示で未取込と区別）
└ 食事ブロック（同型: 既定=低摂取（主+副合計≤6 目安・要本人確認）と「外出」欠食者のみ、展開で全員表 朝昼夕×主/副+水分ml。
    外出・外泊ブロックとの関係は表示上の併記のみ＝自動連動させない【#6】）
```

- 記号体系（全画面共通・色だけに頼らない）: **↑↑赤=危険高値／↑黄=注意高値／↓黄=注意低値／↓↓青(info)=危険低値／✓=記録済・既読／▲=要注意／‼=最重要／—=未測定（記録なし）／網掛け+「未取込」=データ未着**。しきい値は計画書L40の凡例をそのまま定数化。
- 「誰の・何日の」即認知: 日付はsticky常時表示、氏名は各行先頭で --fw-bold、居室番号併記。
- **出勤者ブロックは置かない（スコープ外裁定【#7】）**。切替後はワースケ/シフトアプリ参照を代替とする。最小受け皿が必要と裁定された場合のみ、日付ヘッダ直下に app_settings 由来の日次テキスト1行（折りたたみ・表示のみ）を追加する（設計余地として確保・既定では実装しない）。

## 3. 無限スクロール実装方式【#3反映】

- **フェッチ**: **初期=直近10日・追加=10日ずつ・RPC1発**に統一（要求原文「直近10日分」に整合。Subagent 1 のRPC定義に従う）。1チャンクの構成データは**7種**（①申し送り ②既読状態 ③バイタル ④食事・水分 ⑤外出・外泊 ⑥継続ピン ⑦日次取込状態）を1レスポンスで受ける。日付降順range条件のみ・全件ロード経路をコードに作らない。旧記述「3系列並列 or RPC1発」は撤回。
- **描画コスト遮断**: 日セクションに `content-visibility:auto; contain-intrinsic-size: auto 800px;` → 画面外の日はレイアウト・描画スキップ。末尾sentinel `<div>` を IntersectionObserver（rootMargin '600px'）で検知→次の10日を追加。
- **DOM上限**: 保持60日分。超過したら**古い側の日セクションをアンマウント**し、先頭に「これより新しい日を再読み込み」バーを置く（データはメモリキャッシュ保持・再表示は再マウントのみ）。60日×約80ノード≒5,000ノード以内（Lighthouse警告閾値未満）。
- **仮想スクロールライブラリ: 不採用を推奨**（変更なし）。根拠: ①可変高カード＋展開/折りたたみ＋sticky日付見出しは仮想化の高さ推定と相性が悪くカクつきの主因になる ②content-visibility＋日単位アンマウントで同目的を依存ゼロで達成 ③engineer-brain [73][74][81]。将来実測でfps不足の時のみ @tanstack/react-virtual を再検討。
- **フォールバック**: content-visibility 非対応環境でもDOM上限60日だけで成立する設計。施設iPadのSafariバージョンでの対応可否は**未検証**（Safari 18以降で対応・要実機確認）。
- **体感指標と測定**: 初期タイムライン描画 <1,000ms（再訪 <400ms）／スクロール60fps・dropped frames <5%／追加10日ロード <500ms。測定は合成データ1万件以上（M-026）で Chrome DevTools Performance＋React Profiler、実機iPadは Safari Web Inspector。数値は完了報告に実測添付（原則9）。

## 4. インクリメンタル検索UI

- デバウンス **250ms**＋**IME composition中は発火しない**（compositionend待ち。日本語入力で必須）。最低2文字（利用者名は1文字可＝33名メモリ内・かな/カナ/部分一致正規化でゼロレイテンシ）。
- 対象切替: セグメント「利用者名／本文／記入者」＋フィルタ行（期間プリセット: 10日/1M/3M/1Y/全期間・重要度・勤務帯・職種タグ）。本文検索の方式（pg_trgm等）は Subagent 1 管轄。
- 結果: 件数「〜n件」先頭表示・一致語 `<mark>`（bg:--c-accent-bg・下線併用=色だけ禁止）・結果行はタイムラインと同一カード部品（タップで該当日のタイムラインへ or カルテへ）。
- 3状態: ローディング=スケルトン3行＋「検索中…」／エラー=「検索できませんでした（通信エラー）。電波状態を確認して再試行してください」＋再試行ボタン／0件=「該当する記録がありません。期間を広げるか別の語をお試しください」＋「期間を全期間に広げる」ボタン。

## 5. 個人カルテビュー

- ヘッダ: 氏名（--fs-xl）・かな・居室・階。到達は明示操作のみ（一覧・タイムライン氏名タップ）＝覗き見配慮。
- 期間セグメント: 2週/1M/3M/6M/1Y（既定2週・cl_karteRange で復元）。
- **バイタル折れ線=自前SVG**（外部チャートライブラリ不使用を確定推奨・変更なし）: 縦積み4パネル（体温／血圧上下2線／脈／SpO2）、`viewBox` 幅100%・高さ160px相当。しきい値帯は半透明 `<rect>`（--c-warn-bg/--c-danger-bg）＋帯端にしきい値数値ラベル（色だけ禁止）。欠測日は線を切る（nullを補間しない＝「記録なし」の誤読防止）。データ点は透明44pxヒットrectでタップ→値ツールチップ。`role="img"`＋aria-label要約＋`<details>`内に同データの表。根拠: 必要なのは線・帯・点のみで、recharts等は+80KB超・トークン色/200%対応の制御が難しい（engineer-brain [73][8]）。
- 食事・水分履歴表: 日付逆順・朝昼夕×主/副0〜10＋水分ml（tabular-nums）。低摂取セル▲+warn色、「外出」文字表示。外出・外泊の当日レコードがあれば行末に「外出」chipを併記（自動連動でなく併記【#6】）。
- 関連申し送り: タイムラインと同一カード部品を本人分フィルタで時系列表示（部品共有=変更増幅の防止 [2]）。

## 6. 入力UX

**共通前提**: 全入力画面は §0.5 の入力解禁フラグ配下（封鎖中はディセーブル＋理由文）。記入主体は §0 の操作者。

- **バイタル一括グリッド**（記録→バイタル・2タップ）: フロアセグメント（1階11名/2階21-22名・cl_vitalsFloor復元）・居室昇順・行高56px。列=居室|氏名|KT|上|下|P|SpO2|再検＋。セルタップで**画面下部固定の自前数値キーパッド**（キー56×64px・gap 8px＝手袋対応）。セル内に**前回値をゴースト表示**（--c-ink3・「前回36.5」）。確定で右隣→行末で次行へ自動送り。入力ルール: KT「365」→36.5自動小数・範囲外（KT34.0-42.0等）は「入力値を確認してください（体温は34.0〜42.0）」インライン表示。確定即時にしきい値判定→記号+色。**1名分ずつ自動保存**（部分更新=multi-device-sync原則3・行単位UPSERT）＋ヘッダに「✓保存済み/↻保存中/⚠未送信n件（オフライン退避→§6.5キュー）」。「未測定のまま次へ」ボタンで—を明示。
- **食事一括**: 同型グリッド。0〜10は**11個のセグメントボタン（各44×44・gap8）**＋「外出」トグル（iPad前提。スマホ縦は1名ずつの詳細画面へフォールバック）。水分は加算式チップ（+100/+150/+200ml）＋数値キーパッド＋現在合計表示＋直前追加の「元に戻す」。当日の外出・外泊レコードがある利用者は行頭に「外出」参考chip表示（**「外出」トグルの自動セットはしない**＝表示併記のみ【#6】）。
- **外出・外泊フォーム**【#6新設】（記録→外出・外泊・2タップ）: 利用者ピッカー（かな絞込・カルテと同一部品）／区分（外出/外泊の2ボタン・文字併記）／開始時刻／終了時刻（**「帰着未定」トグル=end null**）／付添（任意テキスト）。帰着の後追い記入はタイムライン外出ブロックの「帰着」ボタン→時刻入力のみの最小シート（end フィールドの部分更新・他フィールドを送らない=multi-device-sync 専用書き込み分離）。
- **申し送りフォーム**: 勤務帯（現在時刻から自動初期値・変更可）／対象（利用者ピッカー or「スタッフへ（全体）」）／職種タグ（複数選択チップ）／重要度（通常・▲重要・‼最重要の3ボタン記号併記）／継続フラグ（期限日付き・現行「※前シートからの再掲」手作業17回/期を置換）／本文（--fs-base textarea）／記入者（**操作者を初期値・変更可**【#1】）。送信後トースト「登録しました・元に戻す(8秒)」。
- **破壊的操作**: 申し送り削除=確認ダイアログ＋論理削除（枠線dangerボタン・塗りつぶし禁止）。既存値の上書き=保存トーストにUndo。1タップで取り消し不能な操作を作らない。既読は付与のみ明示操作・自動付与なし【#1】。

## 6.5 送信失敗キューと下書きの保持規則【監査#4対応・新設】

矛盾を「レイヤー分離」で解消する。いずれも localStorage プレフィクスは **`cl_` に統一**（`clg_` は廃止・Subagent 1 側の表記も合わせる）。

| レイヤー | キー | 保持規則 | 根拠 |
|---|---|---|---|
| **送信失敗キュー**（確定済み・送信ボタン後） | `cl_sendQueue` | **保持必須**。電波復帰で自動再送→**成功で即削除**。ヘッダ「⚠未送信n件」で可視化 | 記録消失防止（原則4）を PII 残留リスクより優先。認証必須端末・成功即削除で残留を最小化 |
| **入力途中下書き**（未送信の申し送り本文） | `cl_draftNote` | 送信成功 or 明示破棄で**即削除**＋**24時間期限**（期限超過は起動時に自動削除）。1件のみ保持 | 書きかけ消失防止と共有iPad平文残留の折衷。**この規則で良いか承認時に1点確認** |

- 両キーは業務データを含むため、**§9のUI状態ホワイトリスト（原則11）とは別レイヤー**として管理する（原則11の「業務データを保存しない」はUI状態復元の話であり、原則4のデータ保護キューはその例外と明示定義）。
- 破棄導線: 下書きがある状態でフォームを開くと「書きかけを復元しました［続きを書く／破棄する］」（破棄は確認1回）。

## 7. HIG介護現場要件 適合表

| 要件 | 充足方法 |
|---|---|
| 本文17px以上 | 本文=--fs-base(16px)…**ops系統の規約下限どおり本文16px・ラベル13px**。申し送り本文のみ--fs-lg(18px)へ引き上げ、17px要件は本文系で充足（16px運用の可否は承認時に1点確認） |
| 44pxタップ+間隔8 | 全ボタン min-h/w:--tap-min・gap:--tap-gap、グリッドセル56px、キーパッド56×64px、**サマリチップは擬似要素で縦ヒット44px化【#5】**、既読ボタン44px【#1】 |
| 3状態実装 | タイムライン・検索・カルテ・グリッド・外出外泊全画面にローディング（スケルトン）/エラー（原因+対処文）/空（次の行動提示）を実装 |
| 色+記号 | ↑↓✓▲‼—＋「未取込」「帰着未定」文字を全しきい値・状態表示に併記（§2記号体系） |
| コントラスト7:1 | tokens.css 実測済みペアのみ使用・直書き禁止（§8で機械強制） |
| 3タップ導線 | §1のとおり全主要導線2タップ以内（操作者切替・帰着記入は1タップ） |
| エラー文 | 「何が起きた＋次にどうする」定型（例: 保存失敗→§6.5キュー保持＋「電波復帰後に自動再送します」／封鎖中→§0.5理由文） |
| 覗き見配慮 | 一覧は本文2行clamp・バイタルは異常者のみ既定表示・詳細/全員表は明示操作で展開 |
| 破壊的操作の保護 | 削除=確認＋論理削除、上書き=Undo、既読=明示操作のみ・一括既読なし【#1】 |
| リロード現在地 | §9 |

## 8. tokens.css × Tailwind 統合方針

- `src/styles/tokens.css` にトークン正本をコピーし index.css 冒頭で import。`index.html` に `data-ds="ops"`、`data-mode` は設定画面（ライト/ダーク/OS追従）でJS付替え。
- **tailwind.config を extend でなく置換**（kitchen-app は extend だが本アプリは直書き禁止を構造化）:
  - `theme.colors` = `{ bg:'var(--c-bg)', surface:'var(--c-surface)', ink:'var(--c-ink)', …danger-bg 等トークン全量 }` のみ → `text-slate-500` や `#333` 相当のクラスが**存在しなくなる**
  - `theme.fontSize` = fs-2xs〜fs-2xl のvarマップのみ／`spacing` = sp-1〜8のvarマップ＋`tap:'var(--tap-min)'`／`borderRadius` = r-sm/md/lg
- ダークモード: 全色がvar経由なので Tailwind `darkMode` 設定不要（data-mode切替だけで全画面追従）。
- 直書きの機械ガード: arbitrary value（`text-[14px]`・`bg-[#…]`）をCIで `grep -RnE '\[(#[0-9a-fA-F]|[0-9]+px)' src/` により検出しfail。hig-audit critical 0 と併走（サマリチップのヒット領域拡張も検証対象【#5】）。
- 200%文字: トークンがrem基準＋固定高さ禁止（min-heightのみ）で追従。reduced-motion はトークン側対応済み・アニメーションライブラリを入れない（CSS transitionのみ）。

## 9. リロード現在地維持（原則11）【#1・#4反映】

- プレフィクス `cl_`（全キー統一・`clg_` 廃止【#4】）。
- **UI状態ホワイトリスト**: `cl_view`（timeline/record/karte/search/settings）／`cl_recordTab`（vitals/meals/note/outing）／`cl_vitalsFloor`（1/2）／`cl_karteRange`（14d/1m/3m/6m/1y）／`cl_mode`（light/dark/auto）。復元は既知値の完全一致照合・不一致は既定へフォールバック（try/catch・壊れた値で起動不能にしない）。
- **操作者キー（別枠・#1）**: `cl_staffId`（staff_id 数値のみ・氏名なし）。復元は**固定リストでなく staff スナップショットとの動的照合**（不在・無効は操作者ピッカーへ）。業務データに紐づく状態だが操作主体の同定に必須のため、**理由を添えてスコープ例外として承認時に確定**（原則11の但し書きに準拠）。
- **データ保護キー（別枠・#4）**: `cl_sendQueue`／`cl_draftNote`（§6.5の保持規則に従う。UI状態復元には使わない）。
- **保存しない**: 選択中利用者・表示中日付・検索語・カード展開状態・氏名等（§6.5の2キーを除く）。Supabase Auth セッションはSDK標準の保管に従う（kitchen-app前例踏襲・UI状態キーとは別レイヤー）。

---

## 推奨アクション

1. 本改訂版を計画書 §2（Subagent 2）へ反映し、Subagent 1 と次の4点を突合: ①10日チャンクRPCの7種構成【#3】 ②`note_reads(note_id, staff_id)` の書込経路が明示操作のみである契約【#1】 ③入力解禁フラグのキー名とRLS側強制【#2】 ④outings の end null 部分更新契約【#6】
2. 承認時の確認事項を1枚に集約して提示（下記残課題の★印5件）
3. 実装前に §2 レイアウト（外出ブロック・44pxヒットチップ含む）の静的HTMLモック1枚（合成データ）でダーク・200%の Phase 3 視覚検証を先行実施
4. 合成1万件での fps/初期表示の実測ハーネスを Subagent 3 の検証計画に組み込む（変更なし）

## 残課題・要確認（★=承認時に本人確認）

- ★ **認証方式**（共有＋操作者ピッカー vs 個別アカウント）— §0 は共有案前提。個別案なら操作者=Authユーザーに縮退【#1】
- ★ **操作者再確認の頻度**（日替わり＋4時間無操作案の可否）【#1】— 未検証（現場運用に依存）
- ★ **入力途中下書きの24時間保持案**の可否【#4】
- ★ **出勤者ブロックのスコープ外裁定**の承認＋必要時の app_settings 1行受け皿の要否【#7】
- ★ 本文フォント16px（ops規約）と依頼「17px以上」の1px差の裁定（申し送り本文18pxで実質充足させる案）
- content-visibility の施設iPad実機対応（Safari 18未満は最適化が効かない。フォールバックで成立）— 未検証
- 食事「低摂取」しきい値（主+副合計≤6 は仮置き）・水分の記録運用・職種タグ/重要度の語彙 — 本人確認
- スマホ縦での33名一括グリッドは非対応（1名ずつ画面へフォールバック）で良いか
- 切替日D以前の入力封鎖の解除手順（誰が・どこで切り替えるか。app_settings 管理UIの要否）【#2】— Subagent 1 と協議

参照根拠: `/Users/Takeshi/.claude/plans/temporal-rolling-truffle.md`（L33-43 実測構造・L37 付帯ブロック・L40 しきい値凡例・L45-51 ハウス制約・L70-74 未決事項）／`/Users/Takeshi/.claude/design/tokens.css`（--c-surface2・--fs-2xs〜2xl・--tap-min/gap 等の実在を確認済み）／`/Users/Takeshi/.claude/rules/dev-principles.md`（原則4・11・12）／`/Users/Takeshi/Claude/Projects/kitchen-app/.claude/worktrees/remote-control-65a07c/tailwind.config.js`／engineer-brain 01/08（[2][8][73][74][81]）
