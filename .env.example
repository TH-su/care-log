# ── フロントエンド用（VITE_ 付き＝公開バンドルへ焼き込まれる。秘密・個人情報を置かない） ──
# anon key は RLS 前提の公開可能キー（service_role は絶対にここへ書かない・使わない）
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ── ローカルツール専用（seed / importer / backup。VITE_ を付けない＝バンドルに入らない） ──
# Supabase の Session pooler 接続文字列。リポジトリ・チャットに貼らないこと
SUPABASE_DB_URL=
